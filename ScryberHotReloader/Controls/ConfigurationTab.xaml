<UserControl x:Class="ScryberHotReloader.Controls.ConfigurationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ScryberHotReloader.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#1e1e1e">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- Page Settings Section -->
            <Border Background="#252526" BorderBrush="#444" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Page Settings" FontSize="16" FontWeight="SemiBold" Foreground="#007ACC" Margin="0,0,0,15"/>

                    <!-- Page Size -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Page Size:" Foreground="#D4D4D4" VerticalAlignment="Center" Grid.Column="0"/>
                        <ComboBox x:Name="PageSizeComboBox" Grid.Column="1" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5">
                            <ComboBoxItem Content="A4" IsSelected="True"/>
                            <ComboBoxItem Content="Letter"/>
                            <ComboBoxItem Content="Legal"/>
                            <ComboBoxItem Content="A3"/>
                            <ComboBoxItem Content="A5"/>
                            <ComboBoxItem Content="Custom"/>
                        </ComboBox>
                    </Grid>

                    <!-- Page Orientation -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Orientation:" Foreground="#D4D4D4" VerticalAlignment="Center" Grid.Column="0"/>
                        <ComboBox x:Name="OrientationComboBox" Grid.Column="1" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5">
                            <ComboBoxItem Content="Portrait" IsSelected="True"/>
                            <ComboBoxItem Content="Landscape"/>
                        </ComboBox>
                    </Grid>

                    <!-- Custom Page Dimensions -->
                    <StackPanel x:Name="CustomDimensionsPanel" Visibility="Collapsed" Margin="0,10,0,0">
                        <TextBlock Text="Custom Page Dimensions (in points, 72 points = 1 inch)" Foreground="#999" FontSize="11" Margin="0,0,0,5"/>

                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Width (points):" Foreground="#D4D4D4" VerticalAlignment="Center" Grid.Column="0"/>
                            <TextBox x:Name="PageWidthTextBox" Grid.Column="1" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5" Text="595"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Height (points):" Foreground="#D4D4D4" VerticalAlignment="Center" Grid.Column="0"/>
                            <TextBox x:Name="PageHeightTextBox" Grid.Column="1" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5" Text="842"/>
                        </Grid>
                    </StackPanel>

                    <!-- Margins Section -->
                    <TextBlock Text="Margins (in points)" FontWeight="SemiBold" Foreground="#D4D4D4" Margin="0,15,0,10"/>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Top Margin -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,5">
                            <TextBlock Text="Top:" Foreground="#D4D4D4" Margin="0,0,0,3"/>
                            <TextBox x:Name="MarginTopTextBox" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5" Text="72"/>
                        </StackPanel>

                        <!-- Bottom Margin -->
                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,5">
                            <TextBlock Text="Bottom:" Foreground="#D4D4D4" Margin="0,0,0,3"/>
                            <TextBox x:Name="MarginBottomTextBox" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5" Text="72"/>
                        </StackPanel>

                        <!-- Left Margin -->
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Left:" Foreground="#D4D4D4" Margin="0,0,0,3"/>
                            <TextBox x:Name="MarginLeftTextBox" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5" Text="72"/>
                        </StackPanel>

                        <!-- Right Margin -->
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock Text="Right:" Foreground="#D4D4D4" Margin="0,0,0,3"/>
                            <TextBox x:Name="MarginRightTextBox" Background="#1e1e1e" Foreground="#D4D4D4" BorderBrush="#444" Padding="5" Text="72"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Font Settings Section -->
            <Border Background="#252526" BorderBrush="#444" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Font Settings" FontSize="16" FontWeight="SemiBold" Foreground="#007ACC" Margin="0,0,0,15"/>

                    <TextBlock Text="Custom Font Paths" Foreground="#D4D4D4" Margin="0,0,0,5"/>
                    <TextBlock Text="Add paths to custom font files (TTF, OTF). One path per line." Foreground="#999" FontSize="11" Margin="0,0,0,5"/>

                    <TextBox x:Name="FontPathsTextBox"
                             Background="#1e1e1e"
                             Foreground="#D4D4D4"
                             BorderBrush="#444"
                             Padding="8"
                             Height="120"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- External Packages Section -->
            <Border Background="#252526" BorderBrush="#444" BorderThickness="1" CornerRadius="4" Padding="15">
                <StackPanel>
                    <TextBlock Text="External Packages" FontSize="16" FontWeight="SemiBold" Foreground="#007ACC" Margin="0,0,0,15"/>

                    <TextBlock Text="Loaded Assemblies:" Foreground="#D4D4D4" Margin="0,0,0,5"/>
                    <TextBlock Text="These assemblies are available in your C# models. Configure in appsettings.json." Foreground="#999" FontSize="11" Margin="0,0,0,10"/>

                    <Border Background="#1e1e1e" BorderBrush="#444" BorderThickness="1" CornerRadius="3" Padding="10">
                        <ScrollViewer Height="150" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="LoadedAssembliesList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Foreground="#9CDCFE" FontFamily="Consolas" FontSize="11" Margin="0,2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <TextBlock Margin="0,10,0,5">
                        <Run Text="To add external packages, edit " Foreground="#D4D4D4"/>
                        <Run Text="appsettings.json" Foreground="#007ACC" FontWeight="SemiBold"/>
                        <Run Text=" in the application directory." Foreground="#D4D4D4"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
